{
  "name": "bpm-tap",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "PWA per misurare i BPM battendo sul retro dell'iPhone (accelerometro) o sullo schermo.",
  "scripts": {
    "test": "node --test test/*.test.js",
    "serve": "python3 -m http.server 8080"
  }
}
